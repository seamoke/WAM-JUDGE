# LingBot-VA Code Migration Manifest

> Historical migration record. Use `auto_pipline_readme.md` for a new server.

Created: 2026-07-22

## Source and destination

- Source namespace: `cs-zhaoqc-74l4`
- Source Pod: `webagent-cpu-79cb8cddff-fqffg`
- Source root: `/workspace/lingbot-va`
- Destination SSH: `root@127.0.0.1:2222`
- Destination root: `/inspire/hdd/project/sais-auto-scientist/public/Lingbot-va`

## Included

- LingBot-VA source: `wan_va`, `evaluation`, `script`, `robotwin_critic`
- Project metadata and documentation: `README.md`, `INSTALL.md`, `pyproject.toml`,
  `requirements.txt`, `uv.toml`, licenses, paper, examples, and README assets
- RoboTwin source snapshot under `third_party/RoboTwin`
- Curobo source and runtime content under `third_party/RoboTwin/envs/curobo`
- Easy and Hard seed caches under `train_out/robotwin/eval_seed_cache`

## Explicitly excluded

- Model weights and checkpoints: `models`, `checkpoints`
- Training datasets and bulk outputs: all of `train_out` except seed caches
- Logs, virtual environments, runtime tarballs, wheel caches, and old archives
- Git object databases, generated bytecode, compiled CUDA/Python extensions, and backups
- RoboTwin generated `eval_result` and the machine-specific `assets` symlink

These exclusions are intentional. Weights, datasets, and RoboTwin simulation assets are
downloaded or linked separately on the destination machine.

## Source revisions and retained modifications

- RoboTwin commit: `2eeec322d95799f537cbfe5f291a8220d965ccb8`
- Curobo commit: `0a50de1ba72db304195d59d9d0b1ed269696047f`
- RoboTwin `envs/place_object_scale.py`: initialize `arm_tag` during actor loading so
  `check_success` is valid before the first `play_once` call.
- RoboTwin `script/_install.sh`: build PyTorch3D with `--no-build-isolation`.
- RoboTwin `script/requirements.txt`: do not downgrade PyTorch; use
  `huggingface_hub==0.36.2`.
- Curobo `world_mesh.py`: use the current Warp API `wp.device_from_torch`.

## Required runtime alignment

- Python 3.10.16
- PyTorch 2.9.0 with CUDA 12.6 wheels
- Transformers 4.55.2
- Diffusers 0.36.0
- SAPIEN 3.0.0b1
- MPLib 0.2.1
- Training model `transformer/config.json`: `attn_mode` must be `flex`.
- Evaluation model `transformer/config.json`: `attn_mode` must be `torch` or
  `flashattn`.
